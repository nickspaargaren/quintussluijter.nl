name: Pipeline
on: push
jobs:
  CI:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - name: Run CI
        uses: devcontainers/ci@v0.3
        with:
          runCmd: |
            deno lint
            deno fmt --check
